---
name: simulated-agent-bootstrap
description: Create a learning-only simulated agent scaffold for this standalone LangGraph playground repository. Compatibility wrapper; canonical platform-neutral skill source lives under `.agents/skills/simulated-agent-bootstrap/`.
---

# Simulated Agent Bootstrap (Codex compatibility wrapper)

This Codex skill is a thin compatibility reference. The canonical, platform-neutral skill lives at:

```text
.agents/skills/simulated-agent-bootstrap/SKILL.md
```

When this skill is invoked, load and follow that canonical file instead. Resolve any relative paths, such as `scripts/create_simulated_agent.py`, from `.agents/skills/simulated-agent-bootstrap/`.
