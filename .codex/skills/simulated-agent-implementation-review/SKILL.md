---
name: simulated-agent-implementation-review
description: Review learning-only simulated agent implementations under `simulated_agents/agent_slug/` and create feedback/reference artifacts. Compatibility wrapper; canonical platform-neutral skill source lives under `.agents/skills/simulated-agent-implementation-review/`.
---

# Simulated Agent Implementation Review (Codex compatibility wrapper)

This Codex skill is a thin compatibility reference. The canonical, platform-neutral skill lives at:

```text
.agents/skills/simulated-agent-implementation-review/SKILL.md
```

When this skill is invoked, load and follow that canonical file instead. Resolve any relative paths, such as `references/review-artifacts.md`, from `.agents/skills/simulated-agent-implementation-review/`.
